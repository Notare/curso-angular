import { FiltroArrPipe } from './filtro-arr.pipe';

describe('FiltroArrPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroArrPipe();
    expect(pipe).toBeTruthy();
  });
});
