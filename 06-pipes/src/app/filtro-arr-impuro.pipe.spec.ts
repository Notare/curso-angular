import { FiltroArrImpuroPipe } from './filtro-arr-impuro.pipe';

describe('FiltroArrImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroArrImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
